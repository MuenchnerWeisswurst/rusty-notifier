api:
  url: http://<host>/<rpc interface>
  password: <rpc password>
  update_key: <key>
  login_method: <rpc method for login>
  update_method: <rpc method for data>
telegram:
  token: <telegram bot token>
  chatid: <chat id with the bot>
  retries: <number of retries sending a message via telegram>
  interval: <time to wait between retries in seconds>
storage: <where to store the previous data>
interval: <update interval in seconds>